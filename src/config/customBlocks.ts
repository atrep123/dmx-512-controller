import type { CustomBlockKind } from '@/lib/types'

export const CUSTOM_BLOCK_KIND_ORDER: CustomBlockKind[] = [
  'master-dimmer',
  'scene-button',
  'effect-toggle',
  'fixture-slider',
  'motor-pad',
  'servo-knob',
  'markdown-note',
]
